# Server Environment Configuration Template
# Copy this file to .env and update with your actual values

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=simak_event_attendance

# JWT Secret (for authentication) - generate a secure secret for production
JWT_SECRET=your_jwt_secret_key_here

# Server Configuration
PORT=5000
NODE_ENV=production

# Frontend URL (for CORS) - update with your actual domain
FRONTEND_URL=http://localhost:5000

# Production Examples:
# PORT=5000
# NODE_ENV=production
# FRONTEND_URL=https://your-domain.com
# FRONTEND_URL=https://events.universitas-dumai.ac.id

# Network Configuration (for local network access)
# FRONTEND_URL=http://192.168.1.100:5000